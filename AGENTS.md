# Shopping Scout: 永続ルール

## 読む順番

1. `docs/SHOPPING_SCOUT_SPEC.md` はプロダクト仕様の正本。
2. このファイルは実装時の恒久ルール。
3. `docs/ARCHITECTURE.md` は責務とデータフローの正本。
4. `docs/MILESTONES.md` は機能範囲の正本。
5. `docs/HANDOFF.md` は作業再開時の最新状況。

## プロダクト境界

- 対象は店頭で使う Android Chrome 優先のPWA。
- 中心価値は、ユーザー自身の価格履歴を端末内に育てること。
- 価格データから味、品質、耐久性、健康効果を断定しない。
- 点数を出す将来機能は「価格お得度」であり、総合評価ではない。
- 自分の履歴を市場全体の価格履歴と表現しない。
- 履歴不足では点数を捏造せず、比較保留を明示する。
- 容量・セット内容が違う商品を価格だけで比較しない。
- サプリ等の健康効果はアプリ内で断定しない。

## Phase 1 の制約

- Phase 1 は完全無料で完結させる。
- OpenAI、Gemini、Anthropicの各APIを使わない。
- 有料OCR、有料検索、有料商品DBを使わない。
- 有料クラウド必須の構成にしない。
- 規約違反のスクレイピングを実装しない。
- 画像は原則として端末の外へ送信しない。
- Tesseractの初回言語モデル取得が必要な場合だけ、その事情をUIで説明する。
- 永続化はIndexedDB/Dexieを基本とする。
- ネット相場検索、クラウド同期、共有、課金は現Milestoneの対象外。

## UX ルール

- 片手で扱いやすいAndroid画面を優先する。
- 主な行動ボタンは画面下部に置く。
- ユーザー向け表示にOCR、Worker、IndexedDBなどの技術語を多用しない。
- カメラ拒否、カメラなし、画像読込失敗、OCR失敗を穏やかな文言で扱う。
- 技術的な例外文をそのまま画面に出さない。
- OCR結果は必ず候補。無確認で価格・容量・個数を確定または保存しない。
- 現在価格、通常価格、割引額は独立して確認・修正できるようにする。
- 容量・個数が取れなければ、単価を勝手に補完しない。
- 保存完了後の初回記録は、比較データ不足を説明する。

## OCR と解析

- OCRはブラウザ内Workerで実行し、UIスレッドを塞がない。
- OCR Workerは毎回作り直さず、同一セッションで再利用する。
- 巨大画像は縮小してからOCRするが、文字を潰すほど縮小しない。
- 前処理は最小限の縮小・グレースケール・コントラスト調整に留める。
- OCRの位置、行、confidenceを将来の候補評価に使える型で保持する。
- 価格は単に最大の数字を選ばない。
- 商品番号、日付、容量、個別単価、マイナス額、通常価格を価格と誤認しない。
- 通常価格－割引額と現在価格の整合を候補評価に活用する。
- 解析と単価計算はTypeScriptの純粋関数で行う。
- AIに計算を委ねない。
- fixture固有の商品名を見て答えをハードコードしない。

## データルール

- `Product` と `PriceObservation` を分離する。
- `PriceObservation` には値札時点のpackage snapshotと算出値を保存する。
- schemaVersionを明示し、将来のDexie migrationを追加できる形にする。
- 同一商品はJAN完全一致を最優先する。
- 次に同一店舗＋商品番号の完全一致を使う。
- メーカー名・商品名・容量の曖昧一致では自動統合しない。
- 曖昧一致は、後に「この商品と同じですか？」で確認する設計にする。
- 端末内データの削除、上書き、外部送信は明示的な利用者操作なしに行わない。

## 実装ルール

- TypeScript strictを維持する。
- 依存は必要になった時だけ追加する。
- OpenCV.js、Chart.jsなどを将来のためだけに追加しない。
- UI、OCR、解析、保存の責務を混ぜない。
- I/Oのないロジックを優先してテスト可能にする。
- 既存fixtureと既存テストを削除、skip、弱体化して成功扱いにしない。
- 仕様変更が必要なら、先に仕様書と判断記録を更新する。
- 未確認事項を完成扱いにしない。

## 検証とGit

- 作業開始前後に `git status` を確認する。
- 既存のユーザー変更をreset、checkout、cleanで消さない。
- 変更後は `npm run typecheck`、`npm run test:run`、`npm run build` を実行する。
- lintが導入済みならlintも実行する。
- OCR時間は前処理、初回Worker準備、OCR、解析を分けて測る。
- 実測値には端末・画像・初回モデル取得状態を必ず添える。
- コミットは意味のある単位に分ける。
- pushは権限と既存ワークフローの安全性が確認できる時だけ行う。
- 完了時は `docs/HANDOFF.md` を最新状態にする。
