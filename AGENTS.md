# AGENTS.md

## 役割
- 本リポジトリは「買い物スカウター」Phase 1 開発準備用の共通ルール集です。
- 仕様変更時は `docs/SHOPPING_SCOUT_SPEC.md` を最優先で読み替えること。

## 優先ルール
- `docs/SHOPPING_SCOUT_SPEC.md` が仕様のSingle Source of Truth（正式原本）。
- Android Chrome 対応を前提にする。
- PWA 構成を前提にする（インストール可能・オフライン起動の最低条件を維持）。
- ローカルファースト（端末内処理優先）。
- Phase 1 は完全無料（有料API不使用）。
- 外部価格API・スクレイピングは禁止。
- OpenAI API 及び有料API禁止。
- 規約違反スクレイピングは禁止。
- 画像は原則、外部送信しない。
- OCR結果はそのまま確定しない（確認フローを前提）。
- 価格評価と品質評価を混同しない。
- 自分の価格履歴を「市場価格」と呼ばない。
- 異なる容量/内容量の商品を価格だけで比較しない。
- 健康食品等の効能はアプリ側で断定しない。
- 既存テストを削除して通す行為は禁止。
- TypeScript strict を維持。
- `git status` / `git log --oneline -10` / `git branch` の状況確認を崩さない。

## 開発ルール
- 既存ユーザー変更（既存コミット外の差分）は壊さない。
- 未確認事項は完了扱いしない。
- 機能追加は次の順番を守る（Capture/OCR -> 解析/計算 -> 永続化）。
- `npm run typecheck`, `npm run test:run`, `npm run build` は継続的に実行し結果を記録する。

## 注意
- このリポジトリは引き継ぎ運用を前提としているため、ドキュメント更新を優先して実装に先行させる。
