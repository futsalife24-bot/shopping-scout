# 設計判断ログ

## 1. PWA icon
本番PWAではPNGが推奨だが、初期フェーズはSVGを採用し、最小基盤として要件満足を優先。  
将来リリース前にPNG置換。

## 2. OCR解析ロジック
今フェーズは画像入力を前提とせず、OCR文字列ベースで進める。  
誤認抑止（コード番号、濃度表示）を優先し、過学習的に商品名依存の分岐は避ける。

## 3. 指標の基準
内部単位は「円」「m」「mL」「g」「count」を基本に定義。  
`totalVolume`は ml / `pricePerLiter`は ml を 1000 で換算して算出する。
